version: '3.7'
services:
    app:
      image: gutenberg-org/app
      build: .
      command: npm start
      environment:
        - REACT_APP_API_DOMAIN=https://gutendex.com/
      volumes:
        - ./bin:/app/bin
        - ./src:/app/src
        - ./public:/app/public
        - ./package.json:/app/package.json
        - ./tsconfig.json:/app/tsconfig.json
      ports:
        - 3000:3000
